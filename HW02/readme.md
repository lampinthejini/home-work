# 연락처 관리 웹서비스 TRD

이 문서는 FastAPI 기반 연락처 관리 웹서비스를 구현하기 위한 기술 요구사항 정의서(TRD)입니다.

**Version:** 2.0
**Last Updated:** 2026-07-13


## Version History
- **v1** : 초안 작성 완료
- **v1.1** : 파이썬 프로그램 구동으로 실행 완료
- **v1.2** : 도커 실행 후, 초기에 프로그램 진행할 수 있게끔 완료
- **v2.0** :
    - 도커 컨테이너가 없거나 꺼져 있어도 실행 시 자동으로 생성/시작
    - Alembic 마이그레이션 도입 (스키마 변경 이력 관리)
    - 회원가입 시 비밀번호 확인 불일치 검증 추가
    - 검색 실패 시 아무 반응 없던 문제 수정, "전체" 버튼을 "초기화"로 이름 변경
    - 로그인 세션 만료 도입 (1시간, 사용 중 활동이 있으면 자동 연장) + 화면에 실시간 카운트다운 표시
    - 동시 요청 시 500 에러 대신 409로 처리 (회원가입/카테고리/연락처 등록)
    - 백엔드 검증 에러 메시지 한글화, 오류가 여러 건이면 개수 안내 추가
    - 연락처 목록 조회 성능 개선 (N+1 쿼리 제거)
    - 마지막 남은 카테고리는 삭제할 수 없도록 방지
    - `.env`를 git에서 제거하고 `.env.example` 템플릿으로 전환

## 실행 방법

### 1. 실행 전 준비
- `pip install -r requirements.txt` 로 필요한 패키지를 설치합니다.
- `.env.example` 파일을 복사해서 `.env` 파일을 만듭니다. (`cp .env.example .env`)
- Docker Desktop이 설치되어 실행 중이어야 합니다. (컨테이너 자체는 아래 2번에서 자동으로 만들어집니다)

### 2. run_desktop.py로 실행하기 (추천)
```
python run_desktop.py
```
- `hw02-postgres` 도커 컨테이너가 없으면 자동으로 만들고, 꺼져 있으면 자동으로 켭니다.
- 데이터베이스 마이그레이션(Alembic)을 자동으로 최신 상태까지 적용합니다.
- 데스크톱 창이 뜨면서 바로 서비스를 사용할 수 있습니다.

### 3. 서버만 직접 열어서 시작하기
데스크톱 창 없이 웹 서버만 쓰고 싶을 때 사용합니다.

1. 도커 컨테이너를 먼저 준비합니다. (`run_desktop.py`를 한 번도 실행한 적이 없다면 아래 명령으로 직접 만들어주세요. 이미 만들어져 있다면 건너뛰어도 됩니다.)
   ```
   docker run -d --name hw02-postgres -e POSTGRES_USER=user -e POSTGRES_PASSWORD=password -e POSTGRES_DB=contact_db -p 5432:5432 postgres:16
   ```
2. 서버를 실행합니다.
   ```
   uvicorn contact_app.main:app --reload
   ```
3. 브라우저에서 http://127.0.0.1:8000 으로 접속합니다.

---

## 단위테스트 진행결과

`.venv`와 캐시를 전부 지우고 처음부터 다시 설치한 뒤, `run_desktop.py`가 띄우는 것과 동일한 서버(도커 자동 생성, Alembic 마이그레이션 자동 적용 포함)를 실제로 실행해서, `TRD_연락처관리웹서비스_v3.2.md` 15장의 TC-01~TC-28을 pytest가 아니라 **실제 HTTP 요청으로 하나씩 직접 확인**했습니다. TC-08(서버 재시작 후 유지)은 실제로 서버 프로세스를 껐다 켜서 확인했습니다.

**결과: 28개 전원 통과**

### 15-1. 인증

| TC | 시나리오 | 결과 |
|---|---|---|
| TC-01 | 정상 회원가입 → 201, 기본 카테고리 3개 자동 생성 | ✅ PASS |
| TC-02 | 아이디 중복 가입 → 409 | ✅ PASS |
| TC-03 | 아이디/비밀번호 형식 위반 → 422 | ✅ PASS |
| TC-04 | 정상 로그인 → 200 + Set-Cookie | ✅ PASS |
| TC-05 | 틀린 비밀번호 / 존재하지 않는 아이디 → 401, 동일 문구 | ✅ PASS |
| TC-06 | 로그인 없이 보호 API 접근 → 401 | ✅ PASS |
| TC-07 | 정상 로그아웃 후 같은 쿠키로 보호 API 호출 → 401 | ✅ PASS |
| TC-08 | 서버 재시작 후 재접속 → 로그인 유지, 데이터 그대로 | ✅ PASS |

### 15-2. 연락처

| TC | 시나리오 | 결과 |
|---|---|---|
| TC-09 | 정상 등록 → 201, total +1 | ✅ PASS |
| TC-10 | 이름/전화번호 형식 위반 → 422 | ✅ PASS |
| TC-11 | 없는/남의 category_id 지정 → 404 | ✅ PASS |
| TC-12 | 전화번호 중복(내 연락처 내) → 409 | ✅ PASS |
| TC-13 | 동일 전화번호를 다른 사용자가 등록 → 201 성공 | ✅ PASS |
| TC-14 | 이름 검색(동명이인 2건) → 200, 2건 | ✅ PASS |
| TC-15 | 검색 결과 0건 → 200, 빈 배열 | ✅ PASS |
| TC-16 | B 계정으로 A의 연락처 목록·직접 접근 시도 | ✅ PASS |
| TC-17 | 부분 수정(주소만 변경) → 200, 나머지 그대로 | ✅ PASS |
| TC-18 | 남의 연락처 id로 수정/삭제 → 404 | ✅ PASS |

### 15-3. 카테고리

| TC | 시나리오 | 결과 |
|---|---|---|
| TC-19 | 가입 직후 기본 카테고리 확인 → 200, 3건 | ✅ PASS |
| TC-20 | 정상 추가 / 이름 중복 → 201 / 409 | ✅ PASS |
| TC-21 | 카테고리 신설이 다른 계정에 노출되지 않음 | ✅ PASS |
| TC-22 | 이름 변경 시 소속 연락처의 표시명 연동 → 200 | ✅ PASS |
| TC-23 | 미사용 카테고리 삭제 → 204 | ✅ PASS |
| TC-24 | 사용 중 카테고리 삭제 시도 → 409 | ✅ PASS |

### 15-4. 통합

| TC | 시나리오 | 결과 |
|---|---|---|
| TC-25 | 가입→로그인→등록→수정→삭제→로그아웃 전체 흐름 | ✅ PASS |
| TC-26 | 계정 A·B 완전 격리 검증 | ✅ PASS |
| TC-27 | 모든 실패 케이스 재실행 → 500 없이 401/404/409/422만 | ✅ PASS |
| TC-28 | 이중 제출 방지(빠른 두 번 클릭) | ✅ PASS |

같은 시나리오는 `tests/test_api.py`의 pytest 자동화 테스트로도 항상 재검증할 수 있습니다 (`python -m pytest`).

---

## Version History(TRD)
 **완성 2026-07-09**
- **v1** : 상세 TRD 작성
- **v2** : ADS 형태로 구조 개선
- **v3** : 상세 TRD와 ADS 통합
- **v3.1** : 다이어그램 및 화면 설계 추가
- **v3.2** : 정책, 부록, AI 판단 기준 보완

---

## References

본 문서는 다음 자료를 기반으로 작성되었습니다.

- 과제 목적
- 구현 요구사항
- 화면 정의서
- 기능 정의서
- PRD(Project Requirements Document)

또한 AI(ChatGPT, Claude Code)를 활용하여 문서의 구조를 개선하고, 기술 명세의 일관성을 검토하였습니다.
